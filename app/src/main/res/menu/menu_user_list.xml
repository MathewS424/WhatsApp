<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/menu_user_list_search"
        android:icon="@drawable/icon_search"
        android:title="Search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="collapseActionView|ifRoom"
        />

    <item android:id="@+id/contact_settings"
        android:title="Contact settings"/>
    <item android:id="@+id/invite_friend"
        android:title="Invite a friend"/>
    <item android:id="@+id/user_list_contacts"
        android:title="Contacts"/>
    <item android:id="@+id/user_list_refresh"
        android:title="Refresh"/>
    <item android:id="@+id/user_list_help"
        android:title="Help"/>

</menu>